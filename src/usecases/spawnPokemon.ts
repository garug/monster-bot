export default async () => {
  // TODO
  // const sorted = await sort()

  // return Promise.all([
  //   saveToDynamo(sorted),
  //   sendMessages(sorted.chance, sorted.shiny, sorted.image)
  // ])
}
